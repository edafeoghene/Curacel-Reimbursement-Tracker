node_modules/
dist/
build/
coverage/
.DS_Store
*.log

# Local env — ignore all variants, opt-in only the example
.env*
!.env.example

# Service account credentials — never commit
# Block any *.json that looks like Google service-account credentials specifically.
edafeoghene-project-*.json
*service-account*.json
*-credentials*.json
*.gserviceaccount*.json

# Google OAuth client secret JSON downloaded from Cloud Console — never commit
client_secret_*.json
*.apps.googleusercontent.com.json

# IDE
.vscode/
.idea/
*.swp

# Vitest
.vitest-cache/

# TypeScript incremental build artifacts
*.tsbuildinfo

# Vercel
.vercel/

# Internal planning docs — keep local, don't publish
PLAN.md
NEXT.md
STATUS_LOOKUP.md
TEAM_LEAD_APPROVAL_PLAN.md
FINANCE_HANDOVER.md
frontend/AGENTS.md
frontend/CLAUDE.md
